<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.demo.boardMapper">

    <select id="boardList" resultType="Board" parameterType="Board">
        select id, title, inserted
        FROM Board
        ORDER BY id DESC
    </select>

    <select id="getBoard" resultType="Board">
        SELECT * FROM Board WHERE id = #{id}
    </select>

    <insert id="insertBoard" parameterType="Board">
        INSERT INTO Board VALUES (auto.nextval, #{title}, #{content}, sysdate)
    </insert>

    <update id="updateBoard">
        UPDATE Board SET title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="deleteBoardById">
        DELETE FROM Board WHERE id = #{id}
    </delete>

</mapper>